# Public eval harness assets

Non-sensitive assets only: runner configuration, fixtures, and the
synthetic preflight task for the eval-runner harness.

Sealed materials — holdout tasks, ground-truth notes, external-source
snapshots, the quality rubric, and judge (scorer/verifier) prompts and
configurations — must NEVER be committed here. They form the sealed judge
package that lives outside every evaluated plugin installation
(`docs/conventions.md` §1; pilot plan, "Atomic seal").
