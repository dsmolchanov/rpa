{
  "_comment": "Eval-runner configuration example (pilot plan, prerequisite 5). Copy, fill in real installation dirs + hashes at candidate freeze, and keep the filled copy in the private eval workspace. All three arms must share model/effort per the pinned runtime configuration; only installation content differs.",
  "arms": {
    "baseline": {
      "installation_dir": "/private-eval-workspace/installs/baseline-a7de5f6-plus-overlay",
      "sha256": "<recorded at installation build>",
      "model": "opus",
      "effort": "default"
    },
    "candidate": {
      "installation_dir": "/private-eval-workspace/installs/candidate-<sha>",
      "sha256": "<recorded at installation build>",
      "model": "opus",
      "effort": "default"
    },
    "ablation": {
      "installation_dir": "/private-eval-workspace/installs/ablation-<sha>",
      "sha256": "<recorded at installation build>",
      "model": "opus",
      "effort": "default"
    }
  },
  "backend_cmd": ["claude", "--model", "opus"],
  "timeout_seconds": 3600
}
