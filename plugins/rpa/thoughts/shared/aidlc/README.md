# AI-DLC Index

This directory is optional and secondary.

Use it only for:
- summaries
- pointers to canonical `aidlc-docs/` artifacts
- handoff-friendly indexes

Do not use this directory as a second source of truth for AI-DLC workflow state.
